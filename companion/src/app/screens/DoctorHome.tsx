// Placeholder for the doctor's dashboard, shown once the passcode gate is
// passed. Real content (per-patient diary review, PK/PD-model output) is the
// next increment.
export function DoctorHome() {
  return (
    <div className="rounded-md border border-line bg-surface p-4">
      <h1 className="text-title font-medium">Doctor</h1>
      <p className="mt-2 text-body text-fg">The dashboard comes in the next increment.</p>
    </div>
  );
}
